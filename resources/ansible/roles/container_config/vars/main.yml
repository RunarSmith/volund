---

install_packages:
  archive:
    - bzip2
    - gzip
    # - p7zip
    - tar
    - unzip
    - zip
  devel:
    - autoconf
    - automake
    - cmake
    - gcc
    - gdb
    - git
    - make
  python:
    - python3
  system:
    - bash
    - btop
    - logrotate
    - mise
    - plocate
    - sudo
  utils:
    - binutils
    - bison
    - diffutils
    - dos2unix
    - expect
    - fzf
    - glow
    - gnupg
    - jq
    - less
    - tidy
    - time
    - tk
    - tree
    - yq
  web:
    - curl
    - openssl
    - wget

test_commands:
  - curl --version
  - git --version
  - jq --version
  - man --version
  - openssl version
  # - p7zip --help
  - pip --version
  - pipx --version
  - python3 --version
  - sudo --version
  - tar --version
  - tree --version
  - unzip --help
  - which ssh
  - wget --version
  - yq --version
  - zip -h2
